module.exports = {
  preset: "ts-jest",
  testEnvironment: "node",
  // If you have custom paths in your tsconfig.json, you might need the below configuration as well
  // moduleNameMapper: {
  //   '^@yourAlias/(.*)$': '<rootDir>/yourActualPath/$1',
  // },
};
